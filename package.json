{
  "name": "pokemon-card-shop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-buttons": "node src/generateSnipcartButtons.js",
    "build": "npm run generate-buttons && vite build",
    "dev": "npm run generate-buttons && vite"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "axios": "^1.13.4"
  }
}
